#include <stdio.h>
#include "projeto.h"

int main(){
    int opcao;
    do{
        exibirMenu();
        scanf("%d", &opcao);

        if (opcao == 0){}
        if else (opcao == 2){}
        if else (opcao == 3){}
        else {printf("Opcao invalida\n");}
    }while(opcao != 0);
}
